# Build artifacts
/bin/
/dist/
/reasonix

# Environment / local config (reasonix.example.toml is the shared template)
.env
.env.local
/reasonix.toml

# Editor / OS
.DS_Store
.idea/
.vscode/

# Go
*.test
*.out

# Personal, machine-local memory (project-scoped). Shared memory is REASONIX.md.
REASONIX.local.md
AGENTS.local.md
